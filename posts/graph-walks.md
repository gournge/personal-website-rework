# Random Walks on Graphs

A random walk on a graph is described by a transition matrix \(P\) where \(P_{ij}\) is the probability of stepping from node \(i\) to node \(j\).

If the graph is connected and aperiodic, then powers \(P^k\) converge to a steady‑state distribution as \(k \to \infty\).
